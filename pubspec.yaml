name: loginblocpractice
description: A new Flutter with login BLOC application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^4.0.1
  equatable: ^1.2.0
  user_repository:
    path: user_repository

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true